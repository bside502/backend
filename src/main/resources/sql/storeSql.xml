<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bside.redaeri.store.StoreMapper">
	<select id="getStoreCount" parameterType="HashMap" resultType="int">
		SELECT
			COUNT(*)
		FROM
			store s
		WHERE
			s.user_idx = #{loginIdx}
	</select>
	

	<insert id="insertStoreInfo" parameterType="HashMap">
		INSERT INTO STORE(
			store_name
			, store_type
			, user_idx
		) VALUE (
			#{storeName}
			, #{storeType}
			, #{loginIdx}
		)
	</insert>
	
	<update id="updateStoreInfo" parameterType="HashMap">
		UPDATE STORE
		SET
			store_name = #{storeName}
			, store_type = #{storeType}
		WHERE
			user_idx = #{loginIdx}
	</update> 
</mapper>