package com.example.redaeri.user;

import java.util.Map;

import org.springframework.stereotype.Service;

@Service
public class LoginService {
	
	/*
	 * public Map<String, Object> naverLogin(Map<String, Object> param) { String
	 * baseURL = "https://nid.naver.com/oauth2.0/authorize?"; String responseType =
	 * ""; String clientId = "client_id"; String redirectURI = ""; String state =
	 * ""; }
	 */
}
